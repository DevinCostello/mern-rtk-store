import React from 'react'

const Sizes = ({sizes}) => {
  return (
    <main>
        <p>Sizes:</p>
        {/* {sizes.map((size) => (
                <button className={CreateOptions.size === size ? styles.size_selected : styles.size} key={size} 
                onClick={() => dispatch(setSize(size))}>
                  {size}
                </button>
              ))} */}
    </main>
  )
}

export default Sizes